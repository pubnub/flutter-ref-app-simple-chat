name: flutter_pubnub_chat
description: A new Flutter project.


publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0    
  intl: ^0.17.0 
  pubnub: ^4.0.0-beta.0
  emoji_picker_flutter: ^1.0.5
  flutter_dotenv: ^4.0.0-nullsafety.1
  url_launcher: ^6.0.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/setup/
    - .env
